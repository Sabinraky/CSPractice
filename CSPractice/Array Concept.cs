using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace CSPractice
{
    public class Array_Concept
    {
        /*public static void Main(string[] args) 
        {
            int[] num = new int[4];
            num[0] = 1;
            num[1] = 2;
            num[2] = 3;
            num[3] = 4;
            char[] charArr = { 'A', 'N', 'T', 'M', 'A', 'N' };
            var Student = new [] { "Sam","Saaz","Sue" };
            object[] arr = { "Jes", 43, 101 };

            foreach ( object i in arr )
            {
                Console.WriteLine( "Value : "+ i );
            }


            foreach (var  i in Student)
            {
                Console.WriteLine("Value : " + i);
            }


            string marvel =" ";
            foreach (char i in charArr)
            {
                marvel+= i.ToString();
            }
            Console.WriteLine( marvel );



            // 2D ARRAY

            int row = 3;
            int col = 3;
            

            int [,] arr1 = new int [row,col]  ;
            int number = 0;

            for (int i = 0; i<row; i++)
            {
                for (int j = 0; j<col; j++)
                {
                    Console.WriteLine("Enter Number : ");
                    number = Convert.ToInt32(Console.ReadLine());
                    arr1[i, j] = number;
                }
            }


            for (int k = 0; k < row; k++)
            {
                Console.WriteLine("");
                for (int l = 0; l < col; l++)
                {
                    Console.Write(arr1[k,l]+"\t");
                }
            }
            Console.WriteLine("");





        }*/
    }
}
